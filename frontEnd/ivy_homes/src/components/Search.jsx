import SearchManager from './SearchManager';

export default function Search() {
  return (
    <div className="search-page">
      {/* SearchManager handles absolutely everything */}
      <SearchManager />
    </div>
  );
}